-- generated by cds-compiler version 2.5.2
VIEW ManagerService_CustomerIndustries AS SELECT
  CustomerIndustries_0.name,
  CustomerIndustries_0.code
FROM sap_sdcpmtool_master_CustomerIndustries AS CustomerIndustries_0
WITH ASSOCIATIONS (
  MANY TO MANY JOIN ManagerService_CustomerIndustries_texts AS texts ON (texts.code = code),
  MANY TO ONE JOIN ManagerService_CustomerIndustries_texts AS localized ON (localized.code = code AND localized.locale = SESSION_CONTEXT('LOCALE'))
)