-- generated by cds-compiler version 2.5.2
VIEW localized_ManagerService_ProjectModes AS SELECT
  ProjectModes_0.name,
  ProjectModes_0.code
FROM localized_sap_sdcpmtool_master_ProjectModes AS ProjectModes_0
WITH ASSOCIATIONS (
  MANY TO MANY JOIN ManagerService_ProjectModes_texts AS texts ON (texts.code = code),
  MANY TO ONE JOIN ManagerService_ProjectModes_texts AS localized ON (localized.code = code AND localized.locale = SESSION_CONTEXT('LOCALE'))
)