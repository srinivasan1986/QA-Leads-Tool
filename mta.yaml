_schema-version: "3.1"
ID: sdcqatoolmd
description: A simple CAP project.
version: 1.0.0
modules:
- name: sdcqatoolmd-srv
  type: nodejs
  path: srv
  requires:
  - name: sdcqatoolmd-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
- name: sdcqatoolmd-db-deployer
  type: hdb
  path: db
  requires:
  - name: sdcqatoolmd-db
  parameters:
    buildpack: nodejs_buildpack
- name: sdcqatoolmd-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: sdcqatoolmd-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - testcomsap.zip
      name: testcomsap
      target-path: resources/
    - artifacts:
      - comsapusermanagement.zip
      name: comsapusermanagement
      target-path: resources/
- name: testcomsap
  type: html5
  path: app/com.sap
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: comsapusermanagement
  type: html5
  path: app/usermanagement
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: sdcqatoolmd-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
  properties:
    hdi-service-name: ${service-name}
- name: sdcqatoolmd-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: sdcqatoolmd-html5-srv
    service-plan: app-host
- name: sdcqatoolmd-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: sdcqatoolmd-dest-srv
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install --production
    - npx -p @sap/cds-dk cds build --production
